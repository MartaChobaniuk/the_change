import { EventList } from '../EventList';

export const Wishes = () => {
  return (
    <EventList
      title="Wishes"
      subtitle="Use the search bar and filters to find the perfect match
      for your skills, passions or resources."
    />
  );
};
